# Copy to .env next to compose.yaml on the server and fill in.

# Folder on the attached volume that holds pth_data.db
PTH_DATA_DIR=/mnt/volume_nyc1_XXXXXXXXXX/pth

# Image tag to run. "latest" follows main; pin a release tag (e.g. v1.1) for production.
PTH_VERSION=latest
