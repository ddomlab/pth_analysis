[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pth-sensor"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "crccheck",
    "pyserial",
]

[project.scripts]
pth-sensor = "pth_sensor.cli:main"
